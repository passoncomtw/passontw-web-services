apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 基礎資源
resources:
  - token-admin-web-deployment.yaml
  - token-admin-web-service.yaml

# 鏡像配置（將被 overlay 覆蓋）
images:
  - name: ghcr.io/passontw/token-admin-web
    newTag: placeholder

