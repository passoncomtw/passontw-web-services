apiVersion: v1
kind: Service
metadata:
  name: token-admin-web
  labels:
    app: token-admin-web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: token-admin-web

